# Neovim config

- Never read a `.env` file without asking the user first.
- Config entry: `init.lua`; plugin specs: `lua/custom/plugins/`.
- Local terminal panel and user guide: [docs/project.md](docs/project.md).
- Verification commands (PTY, adoption, reload, shell integration): [docs/project.md](docs/project.md).
- Keep existing WezTerm project tabs and running agent processes intact during testing.
